import fs from 'fs';

// Read PNG and get the first pixel color naively (if it's not compressed too crazily)
// Actually we can just use an npm tiny package to get color or just use sharp if it's installed.
// Or just base64 encode it and I'll see it. No.
console.log("script running")
